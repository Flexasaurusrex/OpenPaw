---
summary: "CLI reference for `openpaw clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `openpaw clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `openpaw clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `openpaw clawbot qr` (same behavior as [`openpaw qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `openpaw clawbot qr` -> `openpaw qr`
