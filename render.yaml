services:
  - type: web
    name: openpaw-live
    runtime: node
    buildCommand: npm install -g pnpm && pnpm install && pnpm build
    startCommand: node dist/entry.js gateway run --bind 0.0.0.0 --port $PORT --allow-unconfigured
    envVars:
      - key: NODE_ENV
        value: production
