services:
  - type: web
    name: openpaw-live
    runtime: node
    buildCommand: echo "Using pre-built dist - no build needed"
    startCommand: node dist/entry.js gateway run --bind 0.0.0.0 --port $PORT
    envVars:
      - key: NODE_ENV
        value: production
